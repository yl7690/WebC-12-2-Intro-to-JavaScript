var randomX = Math.round(Math.random() * 20);
var randomY = Math.round(Math.random() * 21);

function concatinating(x, y) {
  console.log(x + " + " + y + " = " + (x+y));
}

concatinating(randomX, randomY);
